// Define a type RowID and set it equal to number
export type RowID = number;

// Define an interface RowElement with the required fields
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
